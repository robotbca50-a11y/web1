// 01-coding-languages.js - Python, JS, TS, Java, C, C++, Go, Rust, Swift, Kotlin, PHP, Ruby
module.exports = function(add) {

// ===== PYTHON (20 entries) =====
add('py_basics', `Python Basics: Variable x=10, name="Budi". If/elif/else. For i in range(10). While x>0. Print: print("Hello"). Input: name=input("Name: "). Comments: # single. Docstrings: """multi""". Indentation 4 spasi.`, 'coding');
add('py_data', `Python Data Structures: List [1,2,3] mutable ordered. Tuple (1,2) immutable. Dict {"a":1} key:value. Set {1,2,3} unique. List: append,pop,sort,reverse,extend,insert,index,count. Dict: keys,values,items,get,update,pop,setDefault. Set: add,remove,discard,union(|),intersection(&),difference(-).`, 'coding');
add('py_func', `Python Functions: def greet(name,age=25): return f"Hi {name}". *args tuple args. **kwargs dict keyword args. Lambda: f=lambda x:x*2. Type hints: def func(x:int)->str:. Docstring in triple quotes. Nested functions. Global/nonlocal keywords.`, 'coding');
add('py_oop', `Python OOP: class Dog: def __init__(self,name): self.name=name. def bark(self): return "Woof". class Puppy(Dog): def bark(self): return "Yip" (polymorphism). Encapsulation: _private,__mangled. @property getter. @staticmethod no self. @classmethod cls first param. Multiple inheritance with MRO.`, 'coding');
add('py_comprehension', `Python Comprehension: [x**2 for x in range(10)] list. [x for x in range(10) if x%2==0] filtered. {k:v for k,v in items} dict. {x for x in "hello"} set. (x**2 for x in range(10)) generator expression. [x*y for x in range(3) for y in range(3)] nested.`, 'coding');
add('py_file', `Python File I/O: with open("f.txt") as f: data=f.read(). with open("f.txt","w") as f: f.write("text"). f.readlines() for lines. csv.DictReader for CSV. json.load/dump for JSON. pathlib.Path("f").read_text(). os.path.exists(). os.listdir().`, 'coding');
add('py_except', `Python Exceptions: try: code() except ValueError as e: print(e) except (TypeError,KeyError): handle() except Exception as e: catch_all() else: success() finally: cleanup(). Raise: raise ValueError("msg"). Custom: class MyError(Exception): pass. assert x>0 for debugging.`, 'coding');
add('py_decorator', `Python Decorators: @timer def func(): pass. def timer(func): def wrapper(*a,**kw): start=time.time(); result=func(*a,**kw); return result. @property for getters/setters. @staticmethod. @classmethod. @functools.lru_cache memoize. @dataclass auto __init__.`, 'coding');
add('py_generator', `Python Generators: def gen(): yield 1; yield 2. g=gen(); next(g)=1. for x in gen(): loop. yield from other_gen delegation. send(value) to send into generator. throw() to raise inside. Memory efficient: generates one item at a time.`, 'coding');
add('py_async', `Python Async: import asyncio. async def fetch(): await asyncio.sleep(1). asyncio.run(main()). await asyncio.gather(t1,t2) parallel. async for item in async_gen. async with aiohttp.ClientSession() as s:. Event loop management. Task creation.`, 'coding');
add('py_regex', `Python Regex: import re. re.findall(r"\\d+","abc123")=["123"]. re.sub(r"\\s+"," "," hello "). re.search(r"\\w+","hello"). re.match(r"^\\d+$","123"). re.compile(pat) for reuse. Groups (\\d+). Named (?P<n>\\w+). Flags re.I|re.M.`, 'coding');
add('py_requests', `Python HTTP: import requests. r=requests.get(url). r=requests.post(url,json={"k":"v"}). r.status_code. r.json(). r.text. r.headers. requests.get(url,timeout=5). requests.get(url,headers={"Auth":"token"}). Session: s=requests.Session(); s.get().`, 'coding');
add('py_collections', `Python Collections: Counter("hello")=h:1,e:1,l:2,o:1. defaultdict(list) auto-init. namedtuple("Pt",["x","y"]) dot access. deque(maxlen=10) fast pop left. ChainMap merges dicts. OrderedDict (dict ordered since 3.7).`, 'coding');
add('py_itertools', `Python Itertools: chain(a,b) merge iterables. product(range(3),repeat=2) cartesian. combinations([1,2,3],2) choose 2. permutations([1,2,3],2) ordered. groupby(sorted,key) group. islice(iter,n) slice. accumulate([1,2,3]) running sum. zip_longest fills None.`, 'coding');
add('py_datetime', `Python Datetime: from datetime import datetime,timedelta,date. datetime.now(). datetime(2024,1,15,10,30). timedelta(days=7,hours=3). dt.strftime("%Y-%m-%d"). datetime.strptime(s,"%d/%m/%Y"). date.today(). (date(2024,12,25)-date.today()).days.`, 'coding');
add('py_pandas', `Python Pandas: import pandas as pd. df=pd.read_csv("f.csv"). df.head(). df.describe(). df[df.col>5]. df.groupby("cat").agg({"val":"sum"}). df.merge(df2,on="id"). df.apply(lambda x: x*2). df.to_csv("out.csv",index=False). df.pivot_table().`, 'coding');
add('py_flask', `Python Flask: from flask import Flask,jsonify,request. app=Flask(__name__). @app.route("/api",methods=["GET"]) def get(): return jsonify({"ok":True}). @app.route("/post",methods=["POST"]) def post(): data=request.json. app.run(port=5000,debug=True).`, 'coding');
add('py_testing', `Python Testing: import unittest. class TestCalc(unittest.TestCase): def test_add(self): self.assertEqual(1+1,2). pytest: def test_add(): assert 1+1==2. pytest -v test.py. fixtures: @pytest.fixture def data(): return [1,2,3]. mock: @patch("module.func") def test(mock_func).`, 'coding');
add('py_cli', `Python CLI: import argparse. p=argparse.ArgumentParser(). p.add_argument("name"). p.add_argument("-v","--verbose",action="store_true"). args=p.parse_args(). if args.verbose:. python script.py arg1 -v. sys.argv for simple. click library for advanced.`, 'coding');
add('py_oop_advanced', `Python OOP Advanced: Abstract: from abc import ABC,abstractmethod. class Base(ABC): @abstractmethod def func(): pass. Metaclass: class Meta(type): pass. __slots__ for memory. __str__/__repr__ for display. __eq__/__hash__ for comparison. __enter__/__exit__ context manager. __iter__/__next__ iterator.`, 'coding');

// ===== JAVASCRIPT (20 entries) =====
add('js_basics', `JavaScript Basics: const x=10. let y=20. var z=30 (avoid). Template: \`Hello \${name}\`. Ternary: age>18?"adult":"kid". Nullish: x??"default". Optional: obj?.prop?.deep. typeof x. === strict equality. !== strict inequality. void 0 undefined.`, 'coding');
add('js_es6', `JS ES6+: Arrow: const f=(a,b)=>a+b. Destructuring: const {name,age}=person. const [a,b]=arr. Spread: [...arr],{...obj}. Rest: function f(...args){}. for...of loops iterables. for...in keys objects. Object.entries/keys/values. Array.from(). Array.isArray().`, 'coding');
add('js_array', `JS Array Methods: arr.map(x=>x*2) transform. arr.filter(x=>x>5) keep. arr.reduce((a,b)=>a+b,0) accumulate. arr.find(x=>x.id===1) first match. arr.findIndex(x=>x.id===1) index. arr.some(x=>x>5) any. arr.every(x=>x>0) all. arr.flat() flatten. arr.flatMap() map+flat. arr.sort((a,b)=>a-b) sort. arr.slice(start,end) copy. arr.splice(i,n,items) modify. arr.includes(val) check. arr.join(",") to string.`, 'coding');
add('js_object', `JS Objects: const obj={name:"Budi",age:25}. obj.name or obj["name"]. obj.name="Andi" modify. delete obj.name. "name" in obj check. Object.assign(target,source) merge. Object.freeze(obj) immutable. Object.keys(obj). Object.values(obj). Object.entries(obj). Spread: {...obj1,...obj2}.`, 'coding');
add('js_function', `JS Functions: function greet(name){return \`Hi \${name}\`}. const greet=(name)=>\`Hi \${name}\`. IIFE: (function(){})(). Closure: outer returns inner with access to outer vars. Recursion: function fact(n){return n<=1?1:n*fact(n-1)}. Callback pattern. Higher-order: function that takes/returns function.`, 'coding');
add('js_async', `JS Async: async function getData(){const res=await fetch(url);return await res.json()}. Promise: new Promise((resolve,reject)=>{resolve(data)}). Promise.all([p1,p2]) all resolve. Promise.race([p1,p2]) first. Promise.allSettled all finish. try/catch with await. top-level await in modules.`, 'coding');
add('js_dom', `JS DOM: document.getElementById("id"). document.querySelector(".class"). document.querySelectorAll(".class"). el.textContent="text". el.innerHTML="<b>bold</b>". el.classList.add/remove("cls"). el.setAttribute("href",url). el.addEventListener("click",fn). el.remove(). document.createElement("div"). parent.appendChild(child).`, 'coding');
add('js_event', `JS Events: el.addEventListener("click",fn). e.target element clicked. e.preventDefault(). e.stopPropagation(). Events: click,dblclick,mousedown,mouseup,mouseenter,mouseleave,keydown,keyup,submit,change,input,focus,blur,scroll,resize,load,DOMContentLoaded.`, 'coding');
add('js_fetch', `JS Fetch API: const res=await fetch(url). await res.json(). await res.text(). POST: fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}). Error: if(!res.ok) throw Error. AbortController for timeout.`, 'coding');
add('js_closure', `JS Closure: function counter(){let count=0;return{inc:()=>++count,get:()=>count}}. const c=counter(); c.inc(); c.get()=1. Closures capture outer scope. Used for: data privacy, callbacks, event handlers, currying, partial application, module pattern.`, 'coding');
add('js_class', `JS Classes: class Dog{constructor(name){this.name=name} bark(){return "Woof"}}. class Puppy extends Dog{constructor(name,age){super(name);this.age=age}}. Private: #secret field. Static: static count=0. Getters/setters: get name(){return this._name}.`, 'coding');
add('js_error', `JS Error Handling: try{code}catch(e){console.error(e.message)}finally{cleanup}. throw new Error("msg"). Custom: class MyError extends Error{constructor(msg){super(msg)}}. Error types: TypeError,ReferenceError,SyntaxError,RangeError,URIError.`, 'coding');
add('js_module', `JS Modules: export const name="x". export default function func(){}. import func from "./mod". import {a,b} from "./mod". import * as mod from "./mod". Dynamic: const mod=await import("./mod"). CommonJS: module.exports=require().`, 'coding');
add('js_regexp', `JS RegExp: /pattern/flags. const re=/\\d+/g. re.test(str). re.exec(str). re.match(str). str.match(re). str.replace(re,"x"). str.split(re). Flags: g global, i case-insensitive, m multiline. Groups: /(\\d+)-(\\d+)/. Named: (?<year>\\d{4}). Lookahead: (?=x).`, 'coding');
add('js_map_set', `JS Map & Set: const m=new Map(); m.set("key","val"); m.get("key"); m.has("key"); m.delete("key"); m.size. const s=new Set([1,2,3]); s.add(4); s.has(2); s.delete(1); s.size. WeakMap keys must be objects. WeakSet only objects. Map preserves insertion order.`, 'coding');
add('js_proxy', `JS Proxy: const handler={get(obj,prop){return prop in obj?obj[prop]:"default"}}; const p=new Proxy(obj,handler). Traps: get,set,has,deleteProperty,apply,construct. Reflect API mirrors traps. Used for: validation, logging, default values, reactive data.`, 'coding');
add('js_iterator', `JS Iterators & Generators: function* gen(){yield 1;yield 2}. const g=gen(); g.next()={value:1,done:false}. Symbol.iterator protocol. for...of uses iterator. Yield*: delegate to sub-generator. async generators: async function* gen(){await fetch()}.`, 'coding');
add('js_web_apis', `JS Web APIs: localStorage.setItem(k,v). localStorage.getItem(k). sessionStorage. navigator.geolocation.getCurrentPosition(). IntersectionObserver for scroll. MutationObserver for DOM changes. requestAnimationFrame for animations. Web Workers for background threads. Service Workers for offline. Push notifications.`, 'coding');
add('js_destructuring', `JS Advanced Destructuring: const {name:n,age=25}=person default. const {a:{b:c}}=nested deep. const [a,,c]=[1,2,3] skip. const [a,...rest]=[1,2,3] rest. Function params: function({name,age=25}=). Swap: [a,b]=[b,a].`, 'coding');
add('js_performance', `JS Performance: Debounce: wait before executing. Throttle: execute at most once per interval. requestAnimationFrame for animations. Lazy loading: IntersectionObserver. Memoization: cache function results. Event delegation: one listener on parent. Virtual scrolling for large lists.`, 'coding');

// ===== TYPESCRIPT (10 entries) =====
add('ts_basics', `TypeScript Basics: let x:number=5. let s:string="hi". let b:boolean=true. let a:number[]=[1,2,3]. let t:[string,number]=["age",25]. let u:string|number. let n:null|undefined. let v:any="anything" (avoid). let w:unknown="safer any". let e:never="impossible". void for no return.`, 'coding');
add('ts_interface', `TS Interfaces: interface User{id:number;name:string;email?:string}. interface WithTimestamp extends User{createdAt:Date}. interface Func{(x:number):string}. Type: type ID=string|number. Interface merges on redeclare, type does not. Both support extends/intersection.`, 'coding');
add('ts_generics', `TS Generics: function first<T>(arr:T[]):T{return arr[0]}. class Box<T>{val:T}. type Result<T={string}>=T. Generic constraints: <T extends HasId>. Default: <T=unknown>. Utility: Partial<T>,Required<T>,Pick<T,K>,Omit<T,K>,Record<K,V>. Readonly<T>. ReturnType<F>.`, 'coding');
add('ts_types', `TS Utility Types: Partial<T> all optional. Required<T> all required. Pick<T,"a"|"b"> subset. Omit<T,"c"> exclude. Record<K,V> map. Readonly<T> immutable. ReadonlyArray<T>. NonNullable<T>. Extract<T,U>. Exclude<T,U>. Awaited<T> unwrap Promise.`, 'coding');
add('ts_enum', `TS Enums: enum Direction{Up,Down,Left,Right}. enum Color{Red="red",Blue="blue"}. const enum (inlined). String enums for JSON. Numeric enums auto-increment. Reverse mapping: Direction[0]="Up". Use union types instead: type Dir="up"|"down".`, 'coding');
add('ts_narrowing', `TS Type Narrowing: typeof x==="string". x instanceof Date. "name" in obj. Array.isArray(x). Custom: function isUser(x):x is User{return x.id!==undefined}. Assertion function: function assert(v:unknown):asserts v is string{}. Discriminated unions: tag property.`, 'coding');
add('ts_mapped', `TS Mapped Types: type Optional<T>={[K in keyof T]?:T[K]}. type Readonly<T>={readonly[K in keyof T]:T[K]}. type Getters<T>={[K in keyof T as \`get\${Capitalize<K>}\`]:()=>T[K]}. Key remapping with "as". Template literal types: type EventName=\`on\${string}\`.`, 'coding');
add('ts_conditional', `TS Conditional Types: T extends U?X:Y. IsString<T>=T extends string?true:false. infer keyword: type Return<T>=T extends(...args:infer A)=>infer R?R:never. Distributive: (A|B)extends U?X:Y distributes over union. Mapped+conditional for deep transforms.`, 'coding');
add('ts_decorator', `TS Decorators (experimental): function sealed(ctor){Object.seal(ctor)}. function log(target,name,descriptor){}. Class: @sealed. Method: @log. Property: @decorator. Parameter: @decorator. Experimental flags needed. stage 3 proposal.`, 'coding');
add('ts_advanced', `TS Advanced: Branded types: type UserId=string&{__brand:"UserId"}. Template literals: type CSSUnit=\`\${number}\${"px"|"em"|"%"}\`. Variadic tuples: type Concat<T extends any[]>=[...T,string]. Const assertions: const x=[1,2] as const. satisfies operator.`, 'coding');

// ===== JAVA (10 entries) =====
add('java_basics', `Java Basics: public class Main{public static void main(String[] args){System.out.println("Hello")}}. int x=10. String s="hi". boolean b=true. double d=3.14. char c=A. final int MAX=100. System.out.print/println. Scanner sc=new Scanner(System.in); int n=sc.nextInt().`, 'coding');
add('java_oop', `Java OOP: public class Dog{private String name; public Dog(String name){this.name=name;} public String getName(){return name;}}. class Puppy extends Dog. interface Animal{void speak();}. @Override annotation. abstract class Shape{abstract double area();}.`, 'coding');
add('java_collections', `Java Collections: List<String> list=new ArrayList<>(); list.add("x"); list.get(0); list.remove(0); Map<String,Integer> map=new HashMap<>(); map.put("key",1); map.get("key"); Set<Integer> set=new HashSet<>(); Collections.sort(list); Collections.unmodifiableList(list).`, 'coding');
add('java_streams', `Java Streams: list.stream().filter(x->x>5).map(x->x*2).collect(Collectors.toList()). list.stream().sorted().limit(10).forEach(System.out::println). list.stream().reduce(0, Integer::sum). list.stream().collect(Collectors.groupingBy(x->x)). list.stream().distinct().count().`, 'coding');
add('java_exceptions', `Java Exceptions: try{code}catch(IOException e){e.printStackTrace()}catch(Exception e){}finally{cleanup}. throws in method signature. throw new RuntimeException("msg"). Custom: class MyException extends Exception{}. Checked vs unchecked exceptions.`, 'coding');
add('java_generics', `Java Generics: class Box<T>{private T val; public void set(T val){this.val=val;}}. public static <T extends Comparable<T>> T max(List<T> list). Wildcards: List<? extends Number> reads. List<? super Integer> writes. PECS: Producer Extends, Consumer Super.`, 'coding');
add('java_lambda', `Java Lambda: (x,y)->x+y. (s)->System.out.println(s). ()->{return "value"}. Functional interfaces: Predicate<T>, Function<T,R>, Consumer<T>, Supplier<T>. list.forEach(x->System.out.println(x)). list.sort((a,b)->a.compareTo(b)).`, 'coding');
add('java_sql', `Java JDBC: Connection conn=DriverManager.getConnection(url,user,pass). PreparedStatement ps=conn.prepareStatement("SELECT * FROM users WHERE id=?"); ps.setInt(1,id); ResultSet rs=ps.executeQuery(); while(rs.next()){rs.getString("name")}. Use try-with-resources.`, 'coding');
add('java_spring', `Java Spring Boot: @RestController. @GetMapping("/api") public List<User> getAll(). @PostMapping public User create(@RequestBody User user). @Autowired injection. @Service/@Repository/@Component. application.properties for config. @Value for property injection.`, 'coding');
add('java_concurrent', `Java Concurrency: Thread t=new Thread(()->code()); t.start(). ExecutorService ex=Executors.newFixedThreadPool(4); ex.submit(task). synchronized method/block. ReentrantLock. ConcurrentHashMap. AtomicInteger. CompletableFuture.supplyAsync(()->data).thenAccept(result->).`, 'coding');

// ===== C/C++ (10 entries) =====
add('c_basics', `C Basics: #include <stdio.h>. int main(){printf("Hello"); return 0;}. int x=10. float f=3.14. char c=A. int arr[5]={1,2,3}. struct Point{int x;int y;}. sizeof(x). &x address. *ptr dereference. malloc/calloc/free for heap.`, 'coding');
add('c_pointers', `C Pointers: int x=10; int *p=&x; *p=20 changes x. void* generic pointer. int** pp double pointer. Arrays: int a[5]; a[i]=*(a+i). Function pointers: void (*fp)(int)=func; fp(5). Pointer arithmetic: p+1 moves by sizeof(type). NULL check before dereference.`, 'coding');
add('c_memory', `C Memory: Stack: local vars auto freed. Heap: malloc(n*sizeof(int)) allocate. calloc(n,size) zero-init. realloc(ptr,newsize) resize. free(ptr) release. Memory leak: forget free. Dangling pointer: free then use. Buffer overflow: write past array end. Valgrind to detect.`, 'coding');
add('c_struct', `C Structs: struct Person{char name[50]; int age;}; struct Person p={"Budi",25}. typedef struct{} P; P p. struct* sp=malloc(sizeof(struct)). sp->field access. struct copy by value. Pointers for efficiency.`, 'coding');
add('c_file', `C File I/O: FILE*f=fopen("test.txt","r"); if(f!=NULL){char line[100]; while(fgets(line,100,f)!=NULL){}} fclose(f). fopen modes: r read, w write, a append, r+ read+write. fprintf(f,"x=%d",x). fread/fwrite for binary. feof to check end.`, 'coding');
add('cpp_basics', `C++ Basics: #include <iostream>. using namespace std. cout<<"Hello"<<endl. cin>>x. string s="hi". Reference: int& ref=x. Auto: auto x=5. Range for: for(auto& x:vec). nullptr. constexpr int N=10. Lambda: [capture](params){body}.`, 'coding');
add('cpp_stl', `C++ STL: vector<int> v={1,2,3}; v.push_back(4); v.size(); map<string,int> m; m["key"]=1; set<int> s={1,2}; pair<string,int> p={"a",1}; tuple<int,string> t(1,"a"). Algorithms: sort(v.begin(),v.end()). find(). count(). lower_bound().`, 'coding');
add('cpp_classes', `C++ Classes: class Dog{public: Dog(string n):name(n){} string getName()const{return name;} private: string name;}. Inheritance: class Puppy:public Dog. Virtual: virtual void speak()=0 pure virtual. Override: void speak()override. Destructor: virtual ~Dog().`, 'coding');
add('cpp_smart_ptr', `C++ Smart Pointers: unique_ptr<Dog> d=make_unique<Dog>("Rex"). shared_ptr<Dog> s=make_shared<Dog>("Rex"). weak_ptr<Dog> w=s. No manual delete needed. unique: exclusive ownership. shared: reference counted. weak: observe without owning.`, 'coding');
add('cpp_move', `C++ Move Semantics: Dog(Dog&& other) move constructor. Dog& operator=(Dog&& other) move assignment. std::move(x) cast to rvalue. Swap: void swap(Dog& a,Dog& b){Dog t=std::move(a);a=std::move(b);b=std::move(t)}. RVO Return Value Optimization.`, 'coding');

// ===== GO (8 entries) =====
add('go_basics', `Go Basics: package main. import "fmt". func main(){fmt.Println("Hello")}. x:=10 short declare. var x int=10. const PI=3.14. if x>5{}. for i:=0;i<10;i++{}. for range arr{}. for cond{} infinite. switch x{case 1:}. Fallthrough keyword.`, 'coding');
add('go_func', `Go Functions: func add(a,b int)int{return a+b}. Named returns: func divide(a,b float64)(result float64,err error). Variadic: func sum(nums ...int)int. Anonymous: func(x int)int{return x*2}. Defer: defer fmt.Println("end") runs last.`, 'coding');
add('go_struct', `Go Structs: type Person struct{Name string;Age int}. p:=Person{Name:"Budi",Age:25}. p.Name field access. Methods: func(p Person)String()string. Pointer receiver: func(p *Person)SetAge(n int). Interfaces implicitly implemented.`, 'coding');
add('go_interface', `Go Interfaces: type Writer interface{Write(p []byte)(n int,err error)}. Implicit satisfaction: any type with Write method satisfies Writer. Empty interface: interface{} any type. Type assertion: x.(int). Type switch: switch v:=x.(type).`, 'coding');
add('go_concurrency', `Go Concurrency: go func(){}() goroutine. ch:=make(chan int) channel. ch<-5 send. val:=<-ch receive. select{case v:=<-ch: case <-time.After(1*time.Second):}. sync.Mutex for shared state. sync.WaitGroup for waiting.`, 'coding');
add('go_http', `Go HTTP: http.HandleFunc("/api",func(w http.ResponseWriter,r *http.Request){fmt.Fprintf(w,"Hello")}). http.ListenAndServe(":8080",nil). r.Method check. r.URL.Query().Get("key"). json.NewEncoder(w).Encode(data). http.Get(url) for requests.`, 'coding');
add('go_error', `Go Errors: if err!=nil{return fmt.Errorf("failed: %w",err)}. errors.New("msg"). errors.Is(err,Target). errors.As(err,&target). Panic/recover for fatal. Custom: type MyErr struct{Msg string}. Unwrap for chain.`, 'coding');
add('go_generics', `Go Generics (1.18+): func Map[T any](s []T,f func(T)T)[]T{}. type Number interface{int|float64}. Ordered: type Ordered interface{~int|~float64}. Constraints: [T comparable]. [T any].`, 'coding');

// ===== RUST (6 entries) =====
add('rust_basics', `Rust Basics: let x=5; let mut y=10; y=15. const MAX:u32=100. let s="hello". let v=vec![1,2,3]. println!("x={}",x). match x{1=>"one",_=>"other"}. if let Some(v)=opt{v}. while let Some(v)=iter.next(){}.`, 'coding');
add('rust_ownership', `Rust Ownership: let s1=String::from("hi"). let s2=s1 moves ownership. s1 no longer valid. Clone: let s2=s1.clone(). Borrow: let r=&s1 immutable. let r=&mut s1 mutable. Rules: many & OR one &mut. Cannot borrow as mutable while immutable borrow exists.`, 'coding');
add('rust_struct', `Rust Structs: struct Point{x:f64,y:f64}. let p=Point{x:1.0,y:2.0}. Methods: impl Point{fn dist(&self)->f64{}}. Enums: enum Shape{Circle(f64),Rect(f64,f64)}. Pattern matching: match shape{Shape::Circle(r)=>{},_=>{}}. Option<T> Some/None. Result<T,E> Ok/Err.`, 'coding');
add('rust_error', `Rust Errors: Result<T,E> for recoverable. panic!("crash") for unrecoverable. fn divide(a:f64,b:f64)->Result<f64,String>{if b==0.0{Err("zero".into())}else{Ok(a/b)}}. unwrap() panics on Err. expect("msg"). ? operator for propagation. map_err for conversion.`, 'coding');
add('rust_trait', `Rust Traits: trait Drawable{fn draw(&self);}. impl Drawable for Circle{fn draw(&self){println!("circle")}}. Trait bounds: fn draw_all<T:Drawable>(items:&[T]). impl Trait return. dyn Trait for dynamic dispatch. Derive: #[derive(Debug,Clone,PartialEq)].`, 'coding');
add('rust_async', `Rust Async: async fn fetch_data()->Result<String,Error>{Ok("data".into())}. tokio::spawn(async{fetch_data().await}). .await blocks until ready. async move for ownership. Futures: poll-based. Stream for async iterator. Tokio runtime for execution.`, 'coding');

// ===== KOTLIN (6 entries) =====
add('kt_basics', `Kotlin Basics: val x:Int=10 immutable. var y:String="hi" mutable. fun greet(name:String):String="Hi $name". String templates: "$var". if/when expression. for(i in 1..10). while. null safety: var x:String? = null. x?.length safe call. x!! force unwrap. ?: Elvis operator.`, 'coding');
add('kt_data', `Kotlin Data: data class User(val name:String,val age:Int) auto equals/hashCode/toString. sealed class Result{data class Ok(val data:String):Result(); data class Err(val msg:String):Result()}. when(result){is Ok->is Err->}. List(1,2,3) immutable. mutableListOf().`, 'coding');
add('kt_lambda', `Kotlin Lambda: val square={x:Int->x*x}. val add={a:Int,b:Int->a+b}. it parameter in single param. Lambda with receiver: StringBuilder{append("a")}. Higher order: list.map{it*2}. filter{it>5}. fold(0){acc,x->acc+x}.`, 'coding');
add('kt_coroutine', `Kotlin Coroutines: launch{delay(1000);print("done")}. async{fetchData()}. withContext(Dispatchers.IO){networkCall()}. runBlocking{code()}. CoroutineScope. Job cancellation. SupervisorJob. Flow for async sequences.`, 'coding');
add('kt_ext', `Kotlin Extensions: fun String.isEmail():Boolean{return matches(regex)}. List<String>.firstWord(). Operator overloading: operator fun plus(other:User)=User(name+other.name,age+other.age). Companion object for static-like members. Inline functions.`, 'coding');
add('kt_spring', `Kotlin Spring Boot: @RestController class UserCtrl(@Autowired val repo:UserRepo){ @GetMapping("/users") fun getAll():List<User>=repo.findAll(). @PostMapping fun create(@RequestBody user:User)=repo.save(user) }. Application.kt: runApplication<App>().`, 'coding');

// ===== PHP (6 entries) =====
add('php_basics', `PHP Basics: <?php $name="Budi"; echo "Hello $name"; ?>. $arr=[1,2,3]; $arr[]=4 push. $obj=new stdClass(); $obj->prop="val". function greet($name="World"){return "Hi $name";} . array functions: count(),array_push(),array_merge(),array_map().`, 'coding');
add('php_arrays', `PHP Arrays: Indexed: $a=[1,2,3]. Associative: $a=["name"=>"Budi","age"=>25]. $a["name"] access. unset($a["key"]). isset($a["key"]). count($a). array_keys($a). array_values($a). array_filter($a,function($x){return $x>5}). array_map("strtoupper",$a). array_merge($a,$b). sort($a). array_reverse($a).`, 'coding');
add('php_string', `PHP Strings: strlen($s). substr($s,0,5). strpos($s,"lo"). strtolower($s). strtoupper($s). str_replace("old","new",$s). explode(",",$s). implode(",",$a). trim($s). nl2br($s). htmlspecialchars($s) XSS protection. sprintf("x=%d",$x).`, 'coding');
add('php_oop', `PHP OOP: class Dog{private $name; public function __construct($n){$this->name=$n;} public function bark(){return "Woof";}}. class Puppy extends Dog. interface Animal{public function speak();}. abstract class Shape{abstract public function area();}. Traits: use Serializable;.`, 'coding');
add('php_laravel', `PHP Laravel: Route::get("/users",[UserController::class,"index"]). Model: class User extends Model{$fillable=["name","email"]}. Migration: Schema::create("users",function(Blueprint $t){$t->id();$t->string("name");}); artisan make:model User -m. Blade: @foreach($users as $u){{$u->name}}@endforeach.`, 'coding');
add('php pdo', `PHP PDO: $pdo=new PDO("mysql:host=localhost;dbname=test",$user,$pass). $stmt=$pdo->prepare("SELECT * FROM users WHERE id=:id"); $stmt->execute([":id"=>$id]); $user=$stmt->fetch(PDO::FETCH_ASSOC). Transaction: $pdo->beginTransaction(); ... $pdo->commit().`, 'coding');

// ===== RUBY (5 entries) =====
add('ruby_basics', `Ruby Basics: name="Budi"; puts "Hello #{name}". x=10 unless x.nil?. 5.times{|i| puts i}. [1,2,3].each{|x| puts x}. "hello".length. "hello".upcase. {a: 1,b: 2} hash. =>rocket syntax. symbol :name. class Dog; def bark; "Woof"; end; end.`, 'coding');
add('ruby_blocks', `Ruby Blocks: do |x|...end or {|x| ...}. yield to call block. &block parameter. Proc.new. lambda. map{|x|x*2}. select{|x|x>5}. reduce(0){|a,b|a+b}. each_with_index. flat_map. group_by{|x|x%2}. sort_by{|x|x.length}.`, 'coding');
add('ruby_rails', `Ruby on Rails: rails new app. rails generate model User name:string email:string. rails db:migrate. class UsersController < ApplicationController; def index; @users=User.all; end; end. app/views/users/index.html.erb: <% @users.each do |u| %><%= u.name %><% end %> routes: get "/users",to:"users#index".`, 'coding');
add('ruby_metaprogram', `Ruby Metaprogramming: method_missing for dynamic methods. define_method(:name){@name}. eval("code string"). class_eval{code}. send(:method_name,args). instance_variable_get/set. respond_to_missing?. attr_accessor auto getter/setter. Ruby is highly dynamic.`, 'coding');
add('ruby_testing', `Ruby Testing (RSpec): describe User do; it "is valid" do; u=User.new(name:"Budi"); expect(u).to be_valid; end; context "when nil" do; it "is invalid" do; expect(User.new(name:nil)).not_to be_valid; end; end; end. expect(arr).to include(5). expect{code}.to raise_error.`, 'coding');

// ===== SWIFT (5 entries) =====
add('swift_basics', `Swift Basics: let x=10 constant. var y=20 mutable. func greet(name:String)->String{"Hi \(name)"}. Optionals: var x:String? = nil. if let val=x{val}. guard let val=x{return}. x?.length. x! force unwrap. nil coalescing: x ?? "default".`, 'coding');
add('swift_struct', `Swift Structs: struct Point{var x:Double;var y:Double}. let p=Point(x:1,y:2). struct is value type. class is reference type. Extensions: extension Int{var doubled:Int{return self*2}}. Protocol: protocol Drawable{func draw()}.`, 'coding');
add('swift_combine', `Swift Combine: let publisher=Just(1).sink{print($0)}. publisher.map{$0*2}. filter{$0>5}. debounce(for:.seconds(0.5),scheduler:RunLoop.main). @Published var name:String="". sink(receiveValue:). AnyCancellable.`, 'coding');
add('swift_uikit', `Swift UIKit: class ViewController:UIViewController{override func viewDidLoad(){super.viewDidLoad(); let btn=UIButton(type:.system); btn.setTitle("Tap",for:.normal); btn.addTarget(self,action:#selector(tapped),for:.touchUpInside); view.addSubview(btn)}} @objc func tapped(){}.`, 'coding');
add('swift_swiftui', `Swift SwiftUI: struct ContentView:View{var body:some View{VStack{Text("Hello").font(.largeTitle); Button("Tap"){print("tapped")}.foregroundColor(.blue)}}}. @State var count=0. @Binding var value:String. @ObservedObject var vm:ViewModel. NavigationView/List/ForEach.`, 'coding');

// ===== MORE LANGUAGES (15 entries) =====
add('rust_pattern', `Rust Pattern Matching: match value{0=>"zero",1|2=>"low",3..=9=>"mid",_=>"high"}. if let Some(x)=opt{x}. let&[a,b,..]=&slice. destructuring: let Point{x,y}=p. guard: match x{Some(v) if v>0=>v,_=>0}.`, 'coding');
add('go_maps', `Go Maps: m:=make(map[string]int). m["key"]=1. delete(m,"key"). val,ok:=m["key"] exists check. for k,v:=range m{}. SyncMap for concurrent. map literal: map[string]int{"a":1}. Sort keys: keys:=make([]string,0,len(m)).`, 'coding');
add('c_modern', `C Modern Features (C11+): _Generic(x,int:"int",float:"float"). _Alignas(16) alignment. _Atomic for threading. static_assert. Anonymous structs/unions. _Bool. designator: (Point){.x=1,.y=2}. compound literal.`, 'coding');
add('cpp_ranges', `C++20 Ranges: views::filter([](int x){return x>5}). views::transform([](int x){return x*2}). views::take(10). views::drop(5). views::reverse. views::sort. auto result=vec|views::filter(pred)|views::transform(f)|ranges::to<vector>(). Lazy evaluation.`, 'coding');
add('java_records', `Java Records & Sealed (16+): record Point(int x,int y){}. record Color(String name,int r,int g,int b){}. Point p=new Point(1,2); p.x() accessor. Sealed: sealed interface Shape permits Circle,Rect{}. Pattern matching: if(shape instanceof Circle c){c.radius()}.`, 'coding');
add('kt_flow', `Kotlin Flow: fun numbers():Flow<Int>=flow{for(i in 1..10){delay(100);emit(i)}}. numbers().filter{it>5}.map{it*2}.collect{println(it)}. StateFlow for UI state. SharedFlow for events. callbackFlow for callback-based APIs.`, 'coding');
add('php_modern', `PHP 8.0+ Features: Named arguments: func(name:"Budi",age:25). Match expression: match($x){1=>"one",2=>"two"}. Null safe: $obj?->method()?->prop. Union types: int|string. Enum: enum Color{case Red; case Blue;} Fibers for async.`, 'coding');
add('ruby_modern', `Ruby 3.0+ Features: Pattern matching: case {name:"Budi",age:25}; in {name:String,age:Integer=>age}; p age; end. Ractors for parallelism. YJIT JIT compiler. Bundler for gems. Rake tasks. ERB templates.`, 'coding');
add('scala_basics', `Scala Basics: val x:Int=10. var y:String="hi". def greet(name:String):String=s"Hi $name". if/else expression. for{x<-list if x>0}yield x*2. case class User(name:String,age:Int). trait Animal{def speak:String}. object Singleton.`, 'coding');
add('haskell_basics', `Haskell Basics: x::Int; x=10. greet name="Hi "++name. [1,2,3]++[4,5] list concat. map (*2) [1,2,3]. filter (>5) [1..10]. foldl (+) 0 [1,2,3]. Pattern: factorial 0=1; factorial n=n*factorial(n-1). IO monad for side effects.`, 'coding');
add('lua_basics', `Lua Basics: local x=10. local t={a=1,b=2} table. t["c"]=3 or t.c=3. for i=1,10 do print(i) end. for k,v in pairs(t) do end. function greet(name) return "Hi "..name end. if x>5 then...elseif x>0 then...else...end. Metatables for OOP.`, 'coding');
add('r_basics', `R Basics: x<-10. name<-"Budi". c(1,2,3) vector. list(a=1,b="x"). data.frame(name=c("A","B"),age=c(25,30)). mean(x). sum(x). length(x). append(x,4). library(ggplot2). ggplot(df,aes(x,y))+geom_point(). summary(data).`, 'coding');
add('dart_basics', `Dart Basics: var x=10; String s="hi"; int? nullable. async/await. Future<List<User>> getUsers() async{final res=await http.get(url);return parseUsers(res)}. Class: class Dog{String name; Dog(this.name)}. Mixins: mixin Flyable{void fly()}. Null safety.`, 'coding');
add('elixir_basics', `Elixir Basics: x=10 immutable. [1,2,3]++[4]. %{"key"=>"val"} map. fn x->x*2 end anonymous. hd(list) head. tl(list) tail. | operator cons. defmodule User do defstruct name:"",age:0 end. Pipe |> operator. GenServer for state. Supervision tree.`, 'coding');
add('clojure_basics', `Clojure Basics: (def x 10). (defn greet[name](str "Hi "name)). (map #(* %2) [1 2 3]). (filter even? [1 2 3 4]). (reduce + [1 2 3]). Atom: (def a (atom 0))(swap! a inc). Immutable data structures. REPL driven development.`, 'coding');
};
